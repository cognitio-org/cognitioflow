# Secrets and data — NEVER commit these
.env
.env.local
data
data/
*.db

# Python
.venv/
__pycache__/
*.pyc

# macOS
.DS_Store

# Local scratch
*copy.txt
.aider*

# Agent worktrees. `.claude/settings.json` is tracked, so this cannot be a blanket
# .claude/ rule. These are transient checkouts a subagent works in and the harness
# removes them when it finishes; committing one would nest the repository in itself.
.claude/worktrees/
