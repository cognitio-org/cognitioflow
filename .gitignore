# Secrets and data — NEVER commit these
.env
.env.local
data
data/
*.db

# Python
.venv/
__pycache__/
*.pyc

# macOS
.DS_Store

# Local scratch
*copy.txt
.aider*

# Kokoro model assets (330MB + 30MB) — fetched, not committed
voices/
